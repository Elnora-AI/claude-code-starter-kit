Review all changes made in this session using `git diff` and `git status`.

Create a clear, concise commit message following the conventional commits format:
- `feat:` for new features
- `fix:` for bug fixes
- `docs:` for documentation changes
- `chore:` for maintenance tasks
- `refactor:` for code restructuring
- `test:` for adding or updating tests

Stage the relevant files (do not use `git add .` — be specific about which files to stage).

Commit the changes with the message. Do not push unless I explicitly ask you to.
